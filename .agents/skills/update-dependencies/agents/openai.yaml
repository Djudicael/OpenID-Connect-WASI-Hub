interface:
  display_name: "Update Dependencies"
  short_description: "Safely refresh Rust and frontend dependencies"
  default_prompt: "Use $update-dependencies to update this project dependencies and validate native, WASI, and frontend builds."
